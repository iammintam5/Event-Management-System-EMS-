// This service owns only the registration PostgreSQL schema.
// Business models and their first migrations belong to later phases.
generator client {
  provider = "prisma-client"
  output = "../src/generated/prisma"
  moduleFormat = "esm"
}

datasource db {
  provider = "postgresql"
  schemas = ["registration"]
}
